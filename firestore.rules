rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /gabaritos/{gabaritoId} {
      allow read, write: if (request.resource.data.userId == "admin-thiago-euclydes") || (resource.data.userId == "admin-thiago-euclydes");
    }
  }
}
